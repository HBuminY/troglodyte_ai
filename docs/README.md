   git clone https://github.com/hulusiby/troglodyte_ai.git
   cd troglodyte_ai
   ```

2. Install dependencies:
   ```bash
   pip install -r requirements.txt
   ```

### Usage

To start the main AI engine, run:
```bash
python main.py --config configs/default.yaml
